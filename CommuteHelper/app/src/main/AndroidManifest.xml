<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmsc434.commutehelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HistoryGridViewYear"></activity>
        <activity android:name=".HistoryGridView" />
        <activity android:name=".history_listview" />
        <activity android:name=".HistoryMain" />
        <activity android:name=".MakeTripMethod" />
        <activity android:name=".MakeTripDestination" />
        <activity android:name=".MakeTripStart" />
        <activity android:name=".MakeTripInProgress" />
        <activity android:name=".MakeTripComplete" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>